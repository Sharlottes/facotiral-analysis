{
  "scripts": {
    "dev": "npm run build && node dist",
    "build": "rm -rf dist && tsc"
  },
  "dependencies": {
    "@types/benchmark": "^2.1.2",
    "benchmark": "^2.1.4",
    "microtime": "^3.1.1",
    "typescript": "^5.0.4"
  },
  "license": "MIT"
}
